<!--
SANITISATION.md template. The agent fills every {{PLACEHOLDER}} and deletes these comments.
This file ships inside the public demo, so it must describe what was found WITHOUT repeating it:
write "Customer names (column 2 of the inbox sheet)", never the names themselves.
Leave the PDK:SCAN_RESULTS marker exactly as it is: the workflow replaces it with the scan counts.
-->

# How this demo was made safe

This demo was built from a private project with portfolio-demo-kit, an open-source tool. Every decision
below was proposed by the tool and confirmed or changed by the owner. The source project was not published.

## What was found

| # | What (described, not copied) | Group | Decision | Why |
|---|---|---|---|---|
| {{1}} | {{e.g. Customer names and email addresses in the inbox sheet}} | PERSONAL | FAKE | {{Replaced with invented people}} |
| {{2}} | {{e.g. An API key in the settings file}} | SECRET | REMOVE | {{Never shown}} |

Groups: PERSONAL (about people), PROPRIETARY (the client's own know-how or wording), SECRET (keys,
passwords, private links and IDs), SAFE (generic, fine to show), UNSURE (the owner decided).

## Decisions the owner made

{{Bullets, in plain words, summarising the owner's replies in the issue. Do not quote private details.}}

## How the demo data was made

- Invented inputs were generated from the structure of the real data (column names, types and ranges), never by editing real rows.
- {{Edge cases included, e.g. an empty answer, a very long message, a non-English reply.}}
- Outputs labelled "Generated by ..." are real runs of the project's own prompts on those invented inputs.
- Outputs labelled "Illustrative, written by the agent" are written examples.

## Leak scan results

Before this demo was packaged, a scanner compared every file in it against the private project:
exact strings, runs of {{N}} or more words, secret patterns (keys, emails, private links, IDs) and
ten planted "canary" strings that must never appear. As a self-test, it also planted one real
string in a copy of the demo and confirmed it was caught.

<!-- PDK:SCAN_RESULTS -->

## What this demo does NOT show

{{Bullets: things deliberately left out, e.g. the real prompt wording, production volumes, the client's name, steps the owner asked not to mention.}}

## Limits

Automated checks reduce risk; they do not guarantee that nothing sensitive remains. Publishing
was a manual step taken by the owner after reading this demo.
